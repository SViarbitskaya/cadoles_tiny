{% extends 'base.html.twig' %}

{% block body_id 'admin_user_new' %}

{% block main %}
    <h1>Create a new user</h1>

    {# {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i>Create User
        </button>
        <a href="{{ path('admin_users') }}" class="btn btn-link">
            <i class="fa fa-list-alt" aria-hidden="true"></i> Back to List of Users
        </a>
    {{ form_end(form) }} #}

    {{ include('admin/user/_form.html.twig', {
        form: form,
        button_label: 'action.save'|trans,
        include_back_to_home_link: true,
    }, with_context = false) }}
    
{% endblock %}

{% block sidebar %}
    {{ parent() }}
{% endblock %}